/*Направете програма, в която дефинирайте макро - функция, която приема два аргумента (A и B) и да намира колко е 
резултата от (A + B) ^ 2. Демонстрирайте използването на това макро в main функцията. Изпълнете стъпката на препроцесинг 
за тази програма и качете резултата от нея в отделен файл към заданието.*/

#include <stdio.h>
#define SUM(a, b) ((a)+ (b)) * 2

int main() {
    int a = 3, b = 2;

    printf("%d", SUM(a, b));

    return 0;
}